#include "main.h"

/**
 * mul - multiplies two integers
 * @x: The first number is  a multiply
 * @y: The second number to multipy with first number
 * Return: The result of multiplication between x paran and y paran
 */

int mul(int x, int y)

{
	return (x * y);
}
